import React from 'react';

const UserCart = (props) => {

    return (
        <div>
        usercart here
        <button>Complete Purchase</button>
        </div>
    );

};

export default UserCart; 